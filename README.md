# freshtohome_assignment

# StopWatchApp
This project is created with Angular 8

# to set up dev server
2. install dependences - command: `npm install`
3. run dev server - command: `npm run start:local_dev`
4. Go to http://localhost:4789 
